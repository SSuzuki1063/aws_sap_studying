{
  "name": "aws-sap-studying-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run sync:concepts && npm run sync:static && astro dev",
    "sync:concepts": "rsync -a --delete concepts/ public/concepts/",
    "sync:static": "rsync -a --delete css/ public/css/ && rsync -a --delete js/ public/js/ && rsync -a --delete output_images/ public/output_images/ && rsync -a --delete BlackBelt/ public/BlackBelt/",
    "build": "npm run sync:concepts && npm run sync:static && node scripts/generate-data.mjs && astro build",
    "preview": "astro preview",
    "preview:test": "npm run build && astro preview --port 4321",
    "test:e2e": "playwright test",
    "test:e2e:chromium": "playwright test --project=chromium",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "test:e2e:update-snapshots": "playwright test --project=chromium tests/e2e/visual/ --update-snapshots",
    "qa:css-validate": "tsx tests/modules/validators/css-validator.ts",
    "qa:css-validate:pr": "tsx tests/modules/validators/css-validator.ts --pr-mode",
    "qa:css-runtime": "playwright test --project=qa tests/e2e/qa/css-runtime.spec.ts",
    "qa:report": "tsx tests/modules/report/unified-report.ts",
    "qa:all": "npm run qa:css-validate && npm run qa:css-runtime && npm run qa:report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.14.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "astro": "^5.18.0"
  }
}
