# Ship Skill
1. Run any pending tests or validation scripts
2. Stage all changes: `git add -A`
3. Create a descriptive commit message summarizing changes
4. Commit and push to the remote branch
5. Verify GitHub Pages deployment status with: `gh api repos/{owner}/{repo}/pages/builds/latest`
6. Report the deployed URL and summary of changes
