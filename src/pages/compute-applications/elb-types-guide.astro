---
import ResourceLayout from '../../layouts/ResourceLayout.astro';

const frontmatter = {
  title: '',
  pageTitle: 'AWS Elastic Load Balancing (ELB) 完全ガイド - ALB vs NLB',
  category: 'compute-applications',
  categoryLabel: 'コンピュート・アプリケーション',
  subcategory: '',
  slug: 'elb-types-guide',
  pageCss: undefined,
  tocItems: [
    { level: 'h2', id: '結論ファースト', text: '🎯 結論ファースト' },
    { level: 'h3', id: 'ALBApplication-Load-Balancer', text: '🔵 ALB（Application Load Balancer）' },
    { level: 'h3', id: 'NLBNetwork-Load-Balancer', text: '🟢 NLB（Network Load Balancer）' },
    { level: 'h2', id: 'そもそもロードバランサーとは？', text: '📖 そもそもロードバランサーとは？' },
    { level: 'h3', id: '️-たとえ話-大人気レストランの案内係', text: '🍽️ たとえ話：大人気レストランの「案内係」' },
    { level: 'h3', id: 'AWS-での実際の構成イメージ', text: '🔧 AWS での実際の構成イメージ' },
    { level: 'h2', id: 'ALB-と-NLB-—-2つの案内係の違い', text: '🔀 ALB と NLB — 2つの案内係の違い' },
    { level: 'h2', id: '️-動作するレイヤーの違いOSI参照モデル', text: '🏗️ 動作するレイヤーの違い（OSI参照モデル）' },
    { level: 'h3', id: 'OSI参照モデルでの動作レイヤー', text: '📶 OSI参照モデルでの動作レイヤー' },
    { level: 'h3', id: 'レイヤーの違いをたとえると…', text: '💡 レイヤーの違いをたとえると…' },
    { level: 'h2', id: 'ALB-NLB-機能の詳細', text: '📋 ALB・NLB 機能の詳細' },
    { level: 'h3', id: 'Application-Load-Balancer', text: 'Application Load Balancer' },
    { level: 'h3', id: 'Network-Load-Balancer', text: 'Network Load Balancer' },
    { level: 'h2', id: 'ALB-vs-NLB-一覧比較', text: '📊 ALB vs NLB 一覧比較' },
    { level: 'h2', id: 'どんな時にどちらを使う？', text: '🎯 どんな時にどちらを使う？' },
    { level: 'h3', id: 'Webアプリケーション', text: '🌐 Webアプリケーション' },
    { level: 'h3', id: 'ALB-NLB-選択フローチャート', text: '🔀 ALB / NLB 選択フローチャート' },
    { level: 'h2', id: '️-典型的な構成パターン', text: '🏛️ 典型的な構成パターン' },
    { level: 'h3', id: 'ALB-の典型構成-—-Webアプリ-+-マイクロサービス', text: '🔵 ALB の典型構成 — Webアプリ + マイクロサービス' },
    { level: 'h3', id: 'NLB-の典型構成-—-ゲーム-リアルタイム通信', text: '🟢 NLB の典型構成 — ゲーム / リアルタイム通信' },
    { level: 'h2', id: '⚠️-知っておきたい注意ポイント', text: '⚠️ 知っておきたい注意ポイント' },
    { level: 'h3', id: 'コスト感覚', text: '💰 コスト感覚' },
    { level: 'h2', id: '✅-まとめ', text: '✅ まとめ' },
    { level: 'h3', id: 'ALB-を選ぶとき', text: 'ALB を選ぶとき' },
    { level: 'h3', id: 'NLB-を選ぶとき', text: 'NLB を選ぶとき' }
  ],
  prevPage: 'gwlb-guide.html',
  nextPage: 'alb-nlb-privatelink-guide.html',
  pageNum: 28,
  pageTotal: 46,
};

const rawContent = `<style>* {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #4facfe 75%, #43e97b 100%);
            padding: 20px;
            line-height: 1.7;
            color: #374151;
            padding-top: 70px;}

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 25px 70px rgba(0,0,0,0.3);
            overflow: hidden;
        }

        /* ========== ヘッダー ========== */
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            color: white;
            padding: 50px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '⚖️';
            position: absolute;
            font-size: 18em;
            opacity: 0.08;
            top: -40px;
            right: -30px;
        }

        .header h1 {
            font-size: 2.8em;
            margin-bottom: 12px;
            position: relative;
            z-index: 1;
        }

        .header p {
            font-size: 1.25em;
            opacity: 0.95;
            position: relative;
            z-index: 1;
        }

        .content {
            padding: 50px;
        }

        /* ========== 結論ファースト ========== */
        .conclusion-first {
            background: linear-gradient(135deg, #eff6ff 0%, #e0e7ff 100%);
            border-radius: 16px;
            padding: 40px;
            margin-bottom: 50px;
            border-left: 6px solid #667eea;
            position: relative;
        }

        .conclusion-first h2 {
            color: #4338ca;
            font-size: 1.8em;
            margin-bottom: 20px;
        }

        .conclusion-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 20px;
        }

        .conclusion-card {
            background: white;
            border-radius: 12px;
            padding: 24px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.06);
            border-top: 4px solid;
        }

        .conclusion-card.alb { border-top-color: #3b82f6; }
        .conclusion-card.nlb { border-top-color: #10b981; }

        .conclusion-card h3 {
            font-size: 1.2em;
            margin-bottom: 8px;
        }

        .conclusion-card.alb h3 { color: #1d4ed8; }
        .conclusion-card.nlb h3 { color: #059669; }

        .conclusion-card p {
            font-size: 0.95em;
            color: #6b7280;
        }

        /* ========== セクション共通 ========== */
        .section {
            margin-bottom: 55px;
        }

        .section-title {
            font-size: 1.9em;
            color: #1f2937;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 3px solid #e5e7eb;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .section-title .emoji {
            font-size: 1.1em;
        }

        .section-lead {
            font-size: 1.05em;
            color: #6b7280;
            margin-bottom: 30px;
        }

        /* ========== たとえ話セクション ========== */
        .analogy-box {
            background: linear-gradient(135deg, #fefce8 0%, #fef3c7 100%);
            border-radius: 16px;
            padding: 35px;
            margin-bottom: 40px;
            border: 2px solid #fbbf24;
            position: relative;
        }

        .analogy-box h3 {
            color: #92400e;
            font-size: 1.4em;
            margin-bottom: 15px;
        }

        .analogy-box p {
            font-size: 1em;
            color: #78350f;
            line-height: 1.8;
        }

        /* ========== ELB概念図 ========== */
        .elb-overview-diagram {
            background: #f9fafb;
            border-radius: 16px;
            padding: 40px 30px;
            margin-bottom: 40px;
            text-align: center;
        }

        .elb-overview-diagram h3 {
            font-size: 1.3em;
            color: #374151;
            margin-bottom: 25px;
        }

        .diagram-flow {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0;
            flex-wrap: wrap;
        }

        .diagram-node {
            padding: 16px 22px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 0.95em;
            text-align: center;
            min-width: 130px;
        }

        .diagram-node.users {
            background: linear-gradient(135deg, #dbeafe, #bfdbfe);
            color: #1e40af;
            border: 2px solid #93c5fd;
        }

        .diagram-node.elb {
            background: linear-gradient(135deg, #fde68a, #fbbf24);
            color: #78350f;
            border: 2px solid #f59e0b;
            min-width: 160px;
            font-size: 1.05em;
        }

        .diagram-node.server {
            background: linear-gradient(135deg, #d1fae5, #a7f3d0);
            color: #065f46;
            border: 2px solid #6ee7b7;
        }

        .diagram-arrow {
            font-size: 1.6em;
            color: #9ca3af;
            margin: 0 8px;
            flex-shrink: 0;
        }

        .diagram-server-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        /* ========== レストランたとえ図 ========== */
        .restaurant-diagram {
            background: #fffbeb;
            border-radius: 16px;
            padding: 35px;
            margin: 30px 0;
            border: 2px dashed #f59e0b;
        }

        .restaurant-diagram h4 {
            text-align: center;
            color: #92400e;
            font-size: 1.2em;
            margin-bottom: 20px;
        }

        .restaurant-flow {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        .restaurant-node {
            padding: 14px 18px;
            border-radius: 12px;
            text-align: center;
            font-size: 0.9em;
            min-width: 110px;
        }

        .restaurant-node .icon { font-size: 1.8em; display: block; margin-bottom: 5px; }

        .restaurant-node.guest {
            background: #fef3c7;
            border: 2px solid #fbbf24;
            color: #78350f;
        }

        .restaurant-node.host {
            background: #fde68a;
            border: 2px solid #f59e0b;
            color: #78350f;
            font-weight: 700;
        }

        .restaurant-node.chef {
            background: #d1fae5;
            border: 2px solid #6ee7b7;
            color: #065f46;
        }

        .restaurant-arrow {
            font-size: 1.4em;
            color: #d97706;
            margin: 0 6px;
        }

        .chef-group {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .restaurant-label {
            display: flex;
            justify-content: center;
            gap: 30px;
            font-size: 0.85em;
            color: #92400e;
            margin-top: 12px;
        }

        .restaurant-label span {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .label-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            display: inline-block;
        }

        .label-dot.guest-dot { background: #fbbf24; }
        .label-dot.host-dot  { background: #f59e0b; }
        .label-dot.chef-dot  { background: #6ee7b7; }

        /* ========== ALB vs NLB 比較カード ========== */
        .compare-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }

        .lb-card {
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .lb-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.12);
        }

        .lb-card-header {
            padding: 28px 28px 18px;
            color: white;
            text-align: center;
        }

        .lb-card.alb .lb-card-header {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        }

        .lb-card.nlb .lb-card-header {
            background: linear-gradient(135deg, #10b981, #059669);
        }

        .lb-card-header .lb-icon { font-size: 2.4em; display: block; margin-bottom: 8px; }
        .lb-card-header h3 { font-size: 1.5em; margin-bottom: 4px; }
        .lb-card-header .lb-subtitle { opacity: 0.9; font-size: 0.9em; }

        .lb-card-body {
            padding: 28px;
            background: white;
            padding-top: 70px;}

        .lb-feature {
            display: flex;
            align-items: flex-start;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid #f3f4f6;
        }

        .lb-feature:last-child { border-bottom: none; }

        .lb-feature .feat-icon {
            font-size: 1.2em;
            flex-shrink: 0;
            margin-top: 2px;
        }

        .lb-feature .feat-text strong {
            display: block;
            color: #1f2937;
            font-size: 0.95em;
            margin-bottom: 2px;
        }

        .lb-feature .feat-text span {
            color: #6b7280;
            font-size: 0.88em;
        }

        /* ========== ALB/NLB たとえ図 ========== */
        .analogy-compare {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin: 30px 0;
        }

        .analogy-card {
            border-radius: 14px;
            overflow: hidden;
            border: 2px solid;
        }

        .analogy-card.alb-analogy { border-color: #93c5fd; }
        .analogy-card.nlb-analogy { border-color: #6ee7b7; }

        .analogy-card-header {
            padding: 18px 22px;
            text-align: center;
            font-weight: 700;
            font-size: 1.15em;
        }

        .analogy-card.alb-analogy .analogy-card-header {
            background: #dbeafe;
            color: #1e40af;
        }

        .analogy-card.nlb-analogy .analogy-card-header {
            background: #d1fae5;
            color: #065f46;
        }

        .analogy-card-body {
            padding: 22px;
            background: white;
            padding-top: 70px;}

        .analogy-card-body .scene { font-size: 0.95em; line-height: 1.8; }

        .analogy-visual {
            background: #f9fafb;
            border-radius: 10px;
            padding: 18px;
            margin-top: 15px;
            text-align: center;
            font-size: 0.88em;
            color: #4b5563;
        }

        .analogy-visual .vis-flow {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-top: 10px;
            flex-wrap: wrap;
        }

        .vis-box {
            padding: 8px 14px;
            border-radius: 8px;
            font-size: 0.9em;
            font-weight: 600;
        }

        .vis-arrow { color: #9ca3af; font-size: 1.2em; }

        /* ========== レイヤー図解 ========== */
        .layer-diagram {
            background: #f8fafc;
            border-radius: 16px;
            padding: 35px;
            margin: 30px 0;
        }

        .layer-diagram h3 {
            text-align: center;
            color: #374151;
            font-size: 1.3em;
            margin-bottom: 25px;
        }

        .osi-stack {
            max-width: 700px;
            margin: 0 auto;
        }

        .osi-layer {
            display: grid;
            grid-template-columns: 50px 1fr 200px;
            gap: 15px;
            align-items: center;
            padding: 14px 20px;
            border-radius: 10px;
            margin-bottom: 6px;
            font-size: 0.92em;
            transition: transform 0.2s ease;
        }

        .osi-layer:hover { transform: translateX(5px); }

        .osi-layer .layer-num {
            font-weight: 700;
            font-size: 1.2em;
            text-align: center;
        }

        .osi-layer .layer-name { font-weight: 600; }
        .osi-layer .layer-desc { font-size: 0.85em; opacity: 0.85; }

        .osi-layer.layer7 {
            background: linear-gradient(90deg, #dbeafe, #eff6ff);
            color: #1e40af;
            border: 2px solid #93c5fd;
        }

        .osi-layer.layer4 {
            background: linear-gradient(90deg, #d1fae5, #ecfdf5);
            color: #065f46;
            border: 2px solid #6ee7b7;
        }

        .osi-layer.other {
            background: #f3f4f6;
            color: #6b7280;
            border: 1px solid #e5e7eb;
        }

        .layer-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.8em;
            font-weight: 700;
            text-align: center;
        }

        .badge-alb {
            background: #3b82f6;
            color: white;
        }

        .badge-nlb {
            background: #10b981;
            color: white;
        }

        /* ========== 比較テーブル ========== */
        .comparison-table-wrapper {
            overflow-x: auto;
            border-radius: 14px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            margin: 30px 0;
        }

        .comparison-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.92em;
        }

        .comparison-table thead th {
            padding: 16px 20px;
            text-align: left;
            font-weight: 700;
            font-size: 1em;
        }

        .comparison-table thead th:first-child {
            background: #f9fafb;
            color: #374151;
        }

        .comparison-table thead th:nth-child(2) {
            background: #3b82f6;
            color: white;
        }

        .comparison-table thead th:nth-child(3) {
            background: #10b981;
            color: white;
        }

        .comparison-table tbody td {
            padding: 14px 20px;
            border-bottom: 1px solid #f3f4f6;
            vertical-align: top;
        }

        .comparison-table tbody tr:hover {
            background: #f9fafb;
        }

        .comparison-table tbody td:first-child {
            font-weight: 600;
            color: #374151;
            background: #fafafa;
            white-space: nowrap;
        }

        /* ========== ユースケース ========== */
        .usecase-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 25px;
            margin: 25px 0;
        }

        .usecase-card {
            padding: 24px;
            border-radius: 12px;
            background: white;
            border: 2px solid #e5e7eb;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .usecase-card:hover {
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
        }

        .usecase-card.alb-use { border-left: 5px solid #3b82f6; }
        .usecase-card.nlb-use { border-left: 5px solid #10b981; }

        .usecase-card h3, .usecase-card h4 {
            font-size: 1.05em;
            margin-bottom: 8px;
        }

        .usecase-card.alb-use h3, .usecase-card.alb-use h4 { color: #1d4ed8; }
        .usecase-card.nlb-use h3, .usecase-card.nlb-use h4 { color: #059669; }

        .usecase-card p {
            font-size: 0.9em;
            color: #6b7280;
        }

        .usecase-tag {
            display: inline-block;
            padding: 3px 10px;
            border-radius: 12px;
            font-size: 0.78em;
            font-weight: 600;
            margin-top: 10px;
        }

        .usecase-card.alb-use .usecase-tag {
            background: #dbeafe;
            color: #1d4ed8;
        }

        .usecase-card.nlb-use .usecase-tag {
            background: #d1fae5;
            color: #059669;
        }

        /* ========== 判断フローチャート ========== */
        .flowchart {
            background: linear-gradient(135deg, #faf5ff, #f3e8ff);
            border-radius: 16px;
            padding: 35px;
            margin: 30px 0;
            border: 2px solid #d8b4fe;
        }

        .flowchart h3 {
            text-align: center;
            color: #6b21a8;
            font-size: 1.3em;
            margin-bottom: 25px;
        }

        .flow-steps {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0;
        }

        .flow-question {
            background: white;
            border: 2px solid #c084fc;
            border-radius: 14px;
            padding: 18px 28px;
            text-align: center;
            font-weight: 600;
            color: #581c87;
            max-width: 480px;
            width: 100%;
            font-size: 0.95em;
            box-shadow: 0 2px 8px rgba(139, 92, 246, 0.1);
        }

        .flow-branches {
            display: flex;
            gap: 40px;
            margin: 0;
            position: relative;
        }

        .flow-branch {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0;
        }

        .flow-label {
            font-weight: 700;
            font-size: 0.9em;
            padding: 8px 18px;
            border-radius: 20px;
            margin: 8px 0;
        }

        .flow-label.yes { background: #dcfce7; color: #166534; }
        .flow-label.no  { background: #fee2e2; color: #991b1b; }

        .flow-down-arrow {
            font-size: 1.2em;
            color: #a78bfa;
        }

        .flow-result {
            padding: 16px 24px;
            border-radius: 12px;
            font-weight: 700;
            text-align: center;
            font-size: 0.95em;
            min-width: 180px;
        }

        .flow-result.alb-result {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            color: white;
        }

        .flow-result.nlb-result {
            background: linear-gradient(135deg, #10b981, #059669);
            color: white;
        }

        /* ========== 構成図 ========== */
        .architecture-diagram {
            background: #f8fafc;
            border-radius: 16px;
            padding: 35px;
            margin: 30px 0;
        }

        .architecture-diagram h3 {
            text-align: center;
            font-size: 1.3em;
            color: #374151;
            margin-bottom: 25px;
        }

        .arch-row {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0;
            margin-bottom: 10px;
            flex-wrap: wrap;
        }

        .arch-box {
            padding: 12px 18px;
            border-radius: 10px;
            text-align: center;
            font-size: 0.88em;
            font-weight: 600;
            min-width: 100px;
        }

        .arch-box.client {
            background: #fef3c7;
            border: 2px solid #fbbf24;
            color: #78350f;
        }

        .arch-box.alb-box {
            background: #dbeafe;
            border: 2px solid #3b82f6;
            color: #1d4ed8;
        }

        .arch-box.nlb-box {
            background: #d1fae5;
            border: 2px solid #10b981;
            color: #065f46;
        }

        .arch-box.target {
            background: #f3f4f6;
            border: 2px solid #9ca3af;
            color: #374151;
        }

        .arch-arrow {
            font-size: 1.3em;
            color: #9ca3af;
            margin: 0 6px;
        }

        .arch-target-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .arch-note {
            text-align: center;
            font-size: 0.82em;
            color: #9ca3af;
            margin-top: 6px;
        }

        /* ========== ALB ルーティング図 ========== */
        .routing-diagram {
            background: #eff6ff;
            border-radius: 14px;
            padding: 30px;
            margin: 25px 0;
            border: 1px solid #bfdbfe;
        }

        .routing-diagram h4 {
            text-align: center;
            color: #1d4ed8;
            margin-bottom: 20px;
            font-size: 1.1em;
        }

        .routing-rules {
            display: flex;
            flex-direction: column;
            gap: 12px;
            max-width: 700px;
            margin: 0 auto;
        }

        .routing-rule {
            display: flex;
            align-items: center;
            gap: 12px;
            background: white;
            padding: 14px 20px;
            border-radius: 10px;
            font-size: 0.9em;
            box-shadow: 0 1px 4px rgba(0,0,0,0.05);
        }

        .rule-condition {
            background: #dbeafe;
            color: #1d4ed8;
            padding: 6px 14px;
            border-radius: 8px;
            font-weight: 600;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
            white-space: nowrap;
        }

        .rule-arrow { color: #3b82f6; font-size: 1.2em; }

        .rule-target {
            background: #d1fae5;
            color: #065f46;
            padding: 6px 14px;
            border-radius: 8px;
            font-weight: 600;
        }

        /* ========== 注意ポイント ========== */
        .tips-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 25px 0;
        }

        .tip-card {
            padding: 22px;
            border-radius: 12px;
            background: white;
            border: 1px solid #e5e7eb;
        }

        .tip-card h3, .tip-card h4 {
            font-size: 1em;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .tip-card p {
            font-size: 0.9em;
            color: #6b7280;
            line-height: 1.7;
        }

        /* ========== まとめセクション ========== */
        .summary-section {
            background: linear-gradient(135deg, #f0fdf4, #dcfce7);
            border-radius: 16px;
            padding: 40px;
            border: 2px solid #86efac;
        }

        .summary-section h2 {
            color: #166534;
            font-size: 1.7em;
            margin-bottom: 25px;
            text-align: center;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 25px;
        }

        .summary-card {
            background: white;
            border-radius: 12px;
            padding: 25px;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        }

        .summary-card .sum-icon { font-size: 2.2em; margin-bottom: 10px; }
        .summary-card h3 { font-size: 1.1em; margin-bottom: 8px; color: #1f2937; }
        .summary-card p { font-size: 0.9em; color: #6b7280; line-height: 1.7; }

        .summary-one-liner {
            text-align: center;
            margin-top: 30px;
            padding: 22px;
            background: white;
            border-radius: 12px;
            font-size: 1.05em;
            color: #374151;
            box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        }

        .summary-one-liner strong { color: #4338ca; }

        /* ========== レスポンシブ ========== */
        @media (max-width: 768px) {
            .content { padding: 25px; }
            .header h1 { font-size: 1.8em; }
            .conclusion-grid,
            .compare-grid,
            .analogy-compare,
            .usecase-grid,
            .tips-grid,
            .summary-grid {
                grid-template-columns: 1fr;
            }
            .flow-branches { flex-direction: column; gap: 15px; }
            .diagram-flow,
            .restaurant-flow,
            .arch-row {
                flex-direction: column;
                gap: 4px;
            }
            .diagram-arrow,
            .restaurant-arrow,
            .arch-arrow {
                transform: rotate(90deg);
            }
            .diagram-server-group,
            .chef-group,
            .arch-target-group {
                flex-direction: row;
                gap: 6px;
            }
            .osi-layer {
                grid-template-columns: 40px 1fr;
            }
            .osi-layer .layer-desc { display: none; }
            .routing-rule { flex-direction: column; gap: 6px; }
        }</style>
<!-- ========== ヘッダー ========== -->
        <div class="header">
            <h1>⚖️ AWS Elastic Load Balancing (ELB)</h1>
            <p>ALB と NLB — 2つのロードバランサーの違いをレストランのたとえで完全理解</p>
        </div>

        <div class="content">
            <!-- ========== 結論ファースト ========== -->
            <div class="conclusion-first">
                <h2 id="結論ファースト">🎯 結論ファースト</h2>
                <p>ELB（Elastic Load Balancing）は、<strong>大量のアクセスを複数のサーバーに均等に振り分ける交通整理役</strong>です。主に2種類あり、それぞれ得意分野が異なります。</p>
                <div class="conclusion-grid">
                    <div class="conclusion-card alb">
                        <h3 id="ALBApplication-Load-Balancer">🔵 ALB（Application Load Balancer）</h3>
                        <p>「リクエストの<strong>中身を見て</strong>判断する賢い案内係」。URLやヘッダーを読み取り、最適なサーバーへ振り分ける。Web アプリやマイクロサービス向き。</p>
                    </div>
                    <div class="conclusion-card nlb">
                        <h3 id="NLBNetwork-Load-Balancer">🟢 NLB（Network Load Balancer）</h3>
                        <p>「中身は見ずに<strong>超高速で通す</strong>スピード配達員」。IPアドレスとポート番号だけで瞬時に転送。ゲームサーバーやリアルタイム通信向き。</p>
                    </div>
                </div>
            </div>

            <!-- ========== ELBとは？ ========== -->
            <div class="section">
                <h2 id="そもそもロードバランサーとは？" class="section-title"><span class="emoji">📖</span> そもそもロードバランサーとは？</h2>
                <p class="section-lead">ロードバランサーは、大勢のお客さんが一度にやってきても、1台のサーバーに負荷が集中しないように「交通整理」する仕組みです。</p>

                <div class="analogy-box">
                    <h3 id="️-たとえ話-大人気レストランの案内係">🍽️ たとえ話：大人気レストランの「案内係」</h3>
                    <p>
                        想像してください。あなたは大人気レストランのオーナーです。毎晩100組のお客さんが来店します。<br><br>
                        もしシェフが1人しかいなければ、料理が追いつかずお客さんは何時間も待つことに…。<br>
                        そこで<strong>シェフを5人に増やし</strong>、入口に<strong>「案内係（ホスト）」</strong>を置きます。<br><br>
                        案内係は、各シェフの忙しさを見ながら「あなたはA番テーブルへ」「あなたはB番テーブルへ」と<strong>お客さんを均等に振り分けます</strong>。<br>
                        これがまさに<strong>ロードバランサー</strong>の仕事です！
                    </p>
                </div>

                <!-- レストラン図解 -->
                <div class="restaurant-diagram">
                    <h4>🍽️ レストランの案内係 ＝ ロードバランサーの図</h4>
                    <div class="restaurant-flow">
                        <div class="restaurant-node guest">
                            <span class="icon">👥</span>
                            お客さん<br>(大量のリクエスト)
                        </div>
                        <span class="restaurant-arrow">➡️</span>
                        <div class="restaurant-node host">
                            <span class="icon">🧑‍💼</span>
                            案内係<br><strong>(ELB)</strong>
                        </div>
                        <span class="restaurant-arrow">➡️</span>
                        <div class="chef-group">
                            <div class="restaurant-node chef"><span class="icon">👨‍🍳</span>シェフA (サーバー1)</div>
                            <div class="restaurant-node chef"><span class="icon">👨‍🍳</span>シェフB (サーバー2)</div>
                            <div class="restaurant-node chef"><span class="icon">👨‍🍳</span>シェフC (サーバー3)</div>
                        </div>
                    </div>
                    <div class="restaurant-label">
                        <span><span class="label-dot guest-dot"></span> ユーザーからのアクセス</span>
                        <span><span class="label-dot host-dot"></span> ELB が振り分け</span>
                        <span><span class="label-dot chef-dot"></span> EC2 インスタンス</span>
                    </div>
                </div>

                <!-- 技術的な概念図 -->
                <div class="elb-overview-diagram">
                    <h3 id="AWS-での実際の構成イメージ">🔧 AWS での実際の構成イメージ</h3>
                    <div class="diagram-flow">
                        <div class="diagram-node users">👥 インターネット<br>ユーザー</div>
                        <span class="diagram-arrow">➡️</span>
                        <div class="diagram-node elb">⚖️ ELB<br>(ロードバランサー)</div>
                        <span class="diagram-arrow">➡️</span>
                        <div class="diagram-server-group">
                            <div class="diagram-node server">🖥️ EC2 #1</div>
                            <div class="diagram-node server">🖥️ EC2 #2</div>
                            <div class="diagram-node server">🖥️ EC2 #3</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========== ALB vs NLB 概要 ========== -->
            <div class="section">
                <h2 id="ALB-と-NLB-—-2つの案内係の違い" class="section-title"><span class="emoji">🔀</span> ALB と NLB — 2つの案内係の違い</h2>
                <p class="section-lead">AWSには主に2種類のロードバランサーがあります。同じ「案内係」でも、案内の仕方がまったく違います。</p>

                <div class="analogy-compare">
                    <div class="analogy-card alb-analogy">
                        <div class="analogy-card-header">🔵 ALB ＝ コンシェルジュ型の案内係</div>
                        <div class="analogy-card-body">
                            <div class="scene">
                                レストランに来たお客さんに<strong>「何が食べたいですか？」</strong>と聞いて、希望に合わせて案内するタイプ。<br><br>
                                「パスタ」→ イタリアンシェフへ<br>
                                「寿司」→ 和食シェフへ<br>
                                「デザート」→ パティシエへ<br><br>
                                <strong>リクエストの中身を読んで、最適な場所へ振り分けます。</strong>
                            </div>
                            <div class="analogy-visual">
                                📌 ALBのルーティングイメージ
                                <div class="vis-flow">
                                    <span class="vis-box" style="background:#dbeafe;color:#1d4ed8;">/api/*</span>
                                    <span class="vis-arrow">→</span>
                                    <span class="vis-box" style="background:#d1fae5;color:#065f46;">APIサーバー群</span>
                                </div>
                                <div class="vis-flow" style="margin-top:6px;">
                                    <span class="vis-box" style="background:#dbeafe;color:#1d4ed8;">/images/*</span>
                                    <span class="vis-arrow">→</span>
                                    <span class="vis-box" style="background:#fef3c7;color:#78350f;">画像サーバー群</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="analogy-card nlb-analogy">
                        <div class="analogy-card-header">🟢 NLB ＝ 瞬時に席へ通す受付</div>
                        <div class="analogy-card-body">
                            <div class="scene">
                                お客さんの<strong>注文は聞かず</strong>、「整理番号」だけ見て<strong>瞬時に席へ案内</strong>するタイプ。<br><br>
                                中身は一切確認せず、とにかく<strong>超高速</strong>で振り分けます。<br><br>
                                1秒間に何百万人ものお客さんが来ても捌けるほどのスピード。<br><br>
                                <strong>IPアドレスとポート番号だけで瞬時に転送します。</strong>
                            </div>
                            <div class="analogy-visual">
                                ⚡ NLBの転送イメージ
                                <div class="vis-flow">
                                    <span class="vis-box" style="background:#d1fae5;color:#065f46;">IP:Port</span>
                                    <span class="vis-arrow">→</span>
                                    <span class="vis-box" style="background:#d1fae5;color:#065f46;">即座に転送</span>
                                    <span class="vis-arrow">→</span>
                                    <span class="vis-box" style="background:#f3f4f6;color:#374151;">サーバー</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========== 動作レイヤー図解 ========== -->
            <div class="section">
                <h2 id="️-動作するレイヤーの違いOSI参照モデル" class="section-title"><span class="emoji">🏗️</span> 動作するレイヤーの違い（OSI参照モデル）</h2>
                <p class="section-lead">ALBとNLBは、ネットワーク通信の「どの階層」で動作するかが異なります。これが性能と機能の違いに直結します。</p>

                <div class="layer-diagram">
                    <h3 id="OSI参照モデルでの動作レイヤー">📶 OSI参照モデルでの動作レイヤー</h3>
                    <div class="osi-stack">
                        <div class="osi-layer layer7">
                            <span class="layer-num">L7</span>
                            <span class="layer-name">アプリケーション層（HTTP/HTTPS）</span>
                            <span class="layer-badge badge-alb">← ALB はここ</span>
                        </div>
                        <div class="osi-layer other">
                            <span class="layer-num">L6</span>
                            <span class="layer-name">プレゼンテーション層</span>
                            <span class="layer-desc"></span>
                        </div>
                        <div class="osi-layer other">
                            <span class="layer-num">L5</span>
                            <span class="layer-name">セッション層</span>
                            <span class="layer-desc"></span>
                        </div>
                        <div class="osi-layer layer4">
                            <span class="layer-num">L4</span>
                            <span class="layer-name">トランスポート層（TCP/UDP）</span>
                            <span class="layer-badge badge-nlb">← NLB はここ</span>
                        </div>
                        <div class="osi-layer other">
                            <span class="layer-num">L3</span>
                            <span class="layer-name">ネットワーク層</span>
                            <span class="layer-desc"></span>
                        </div>
                        <div class="osi-layer other">
                            <span class="layer-num">L2</span>
                            <span class="layer-name">データリンク層</span>
                            <span class="layer-desc"></span>
                        </div>
                        <div class="osi-layer other">
                            <span class="layer-num">L1</span>
                            <span class="layer-name">物理層</span>
                            <span class="layer-desc"></span>
                        </div>
                    </div>
                </div>

                <div class="analogy-box">
                    <h3 id="レイヤーの違いをたとえると…">💡 レイヤーの違いをたとえると…</h3>
                    <p>
                        <strong>ALB（L7）</strong>は、手紙を<strong>開封して中身を読んでから</strong>配達先を決める郵便局員。「この手紙は請求書だからA課へ」「これはお礼状だからB課へ」と内容で仕分けます。<br><br>
                        <strong>NLB（L4）</strong>は、封筒の<strong>宛名（IP）と部署番号（ポート）だけ見て</strong>、中身を開けずに超高速で届ける配達員。内容を確認しない分、圧倒的に速いのです。
                    </p>
                </div>
            </div>

            <!-- ========== 詳細カード ========== -->
            <div class="section">
                <h2 id="ALB-NLB-機能の詳細" class="section-title"><span class="emoji">📋</span> ALB・NLB 機能の詳細</h2>
                <p class="section-lead">それぞれのロードバランサーが持つ具体的な機能を見てみましょう。</p>

                <div class="compare-grid">
                    <!-- ALB カード -->
                    <div class="lb-card alb">
                        <div class="lb-card-header">
                            <span class="lb-icon">🔵</span>
                            <h3 id="Application-Load-Balancer">Application Load Balancer</h3>
                            <span class="lb-subtitle">レイヤー7 ／ HTTP・HTTPS 特化</span>
                        </div>
                        <div class="lb-card-body">
                            <div class="lb-feature">
                                <span class="feat-icon">🛣️</span>
                                <div class="feat-text">
                                    <strong>パスベースルーティング</strong>
                                    <span>URLパス（/api, /images など）ごとに異なるサーバー群へ振り分け</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🌐</span>
                                <div class="feat-text">
                                    <strong>ホストベースルーティング</strong>
                                    <span>ドメイン名（api.example.com, www.example.com）で振り分け</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🔌</span>
                                <div class="feat-text">
                                    <strong>WebSocket サポート</strong>
                                    <span>リアルタイム双方向通信にも対応</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🔒</span>
                                <div class="feat-text">
                                    <strong>SSL/TLS 終端</strong>
                                    <span>ALBでHTTPS通信を解読し、バックエンドへはHTTPで転送</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">📍</span>
                                <div class="feat-text">
                                    <strong>X-Forwarded-For ヘッダー</strong>
                                    <span>元のクライアントIPアドレスをサーバーに自動転送</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🏗️</span>
                                <div class="feat-text">
                                    <strong>マイクロサービス対応</strong>
                                    <span>コンテナやLambdaなど多様なターゲットに対応</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- NLB カード -->
                    <div class="lb-card nlb">
                        <div class="lb-card-header">
                            <span class="lb-icon">🟢</span>
                            <h3 id="Network-Load-Balancer">Network Load Balancer</h3>
                            <span class="lb-subtitle">レイヤー4 ／ TCP・UDP 特化</span>
                        </div>
                        <div class="lb-card-body">
                            <div class="lb-feature">
                                <span class="feat-icon">⚡</span>
                                <div class="feat-text">
                                    <strong>超低レイテンシー</strong>
                                    <span>マイクロ秒単位の遅延。ALBよりはるかに高速</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">📈</span>
                                <div class="feat-text">
                                    <strong>超高スループット</strong>
                                    <span>毎秒数百万リクエストを処理可能</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">📌</span>
                                <div class="feat-text">
                                    <strong>静的IPアドレス</strong>
                                    <span>固定IPやElastic IPを割り当て可能。ファイアウォール連携に最適</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🔗</span>
                                <div class="feat-text">
                                    <strong>接続レベルのバランシング</strong>
                                    <span>TCPコネクション単位で振り分け（HTTPヘッダーは不使用）</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🔐</span>
                                <div class="feat-text">
                                    <strong>TLSパススルー</strong>
                                    <span>暗号化を解かずにそのままバックエンドへ転送可能</span>
                                </div>
                            </div>
                            <div class="lb-feature">
                                <span class="feat-icon">🎮</span>
                                <div class="feat-text">
                                    <strong>UDP サポート</strong>
                                    <span>ゲーム・IoT・DNS・VoIPなどUDP通信にも対応</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- ALB ルーティング詳細図 -->
                <div class="routing-diagram">
                    <h4>🛣️ ALB のコンテンツベースルーティング例</h4>
                    <div class="routing-rules">
                        <div class="routing-rule">
                            <span class="rule-condition">/api/*</span>
                            <span class="rule-arrow">→</span>
                            <span class="rule-target">API サーバー群</span>
                            <span style="color:#6b7280;font-size:0.85em;">（バックエンドロジック処理）</span>
                        </div>
                        <div class="routing-rule">
                            <span class="rule-condition">/images/*</span>
                            <span class="rule-arrow">→</span>
                            <span class="rule-target">画像サーバー群</span>
                            <span style="color:#6b7280;font-size:0.85em;">（静的コンテンツ配信）</span>
                        </div>
                        <div class="routing-rule">
                            <span class="rule-condition">Host: api.example.com</span>
                            <span class="rule-arrow">→</span>
                            <span class="rule-target">API 専用クラスター</span>
                            <span style="color:#6b7280;font-size:0.85em;">（ドメイン別振り分け）</span>
                        </div>
                        <div class="routing-rule">
                            <span class="rule-condition">Header: Mobile</span>
                            <span class="rule-arrow">→</span>
                            <span class="rule-target">モバイル最適化サーバー</span>
                            <span style="color:#6b7280;font-size:0.85em;">（デバイス別対応）</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========== 比較テーブル ========== -->
            <div class="section">
                <h2 id="ALB-vs-NLB-一覧比較" class="section-title"><span class="emoji">📊</span> ALB vs NLB 一覧比較</h2>
                <p class="section-lead">主要な違いをまとめて確認しましょう。</p>

                <div class="comparison-table-wrapper">
                    <table class="comparison-table">
                        <thead>
                            <tr>
                                <th>比較項目</th>
                                <th>🔵 ALB</th>
                                <th>🟢 NLB</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>動作レイヤー</td>
                                <td>レイヤー7（アプリケーション層）</td>
                                <td>レイヤー4（トランスポート層）</td>
                            </tr>
                            <tr>
                                <td>対応プロトコル</td>
                                <td>HTTP, HTTPS, WebSocket</td>
                                <td>TCP, UDP, TLS</td>
                            </tr>
                            <tr>
                                <td>ルーティング方式</td>
                                <td>URL パス・ホスト名・ヘッダーなど</td>
                                <td>IP アドレス＋ポート番号のみ</td>
                            </tr>
                            <tr>
                                <td>レイテンシー</td>
                                <td>ミリ秒〜（中身解析のため）</td>
                                <td>マイクロ秒〜（超低遅延）</td>
                            </tr>
                            <tr>
                                <td>スループット</td>
                                <td>高い</td>
                                <td>極めて高い（毎秒数百万件）</td>
                            </tr>
                            <tr>
                                <td>静的IP</td>
                                <td>❌ 非対応（DNS名で接続）</td>
                                <td>✅ 対応（Elastic IP 割当可）</td>
                            </tr>
                            <tr>
                                <td>SSL/TLS</td>
                                <td>終端（解読してから転送）</td>
                                <td>パススルー可能（そのまま転送）</td>
                            </tr>
                            <tr>
                                <td>たとえ</td>
                                <td>🧑‍💼 注文を聞いて案内するコンシェルジュ</td>
                                <td>⚡ 番号札だけで通す超速受付</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- ========== ユースケース ========== -->
            <div class="section">
                <h2 id="どんな時にどちらを使う？" class="section-title"><span class="emoji">🎯</span> どんな時にどちらを使う？</h2>
                <p class="section-lead">具体的なユースケースで使い分けを確認しましょう。</p>

                <div class="usecase-grid">
                    <div class="usecase-card alb-use">
                        <h3 id="Webアプリケーション">🌐 Webアプリケーション</h3>
                        <p>ECサイト、SNS、管理画面など HTTP/HTTPS ベースのWebアプリ。URLパスやドメインで振り分けたい場合に最適。</p>
                        <span class="usecase-tag">ALB を選択</span>
                    </div>
                    <div class="usecase-card nlb-use">
                        <h4>🎮 ゲームサーバー</h4>
                        <p>オンラインゲームのリアルタイム通信。超低遅延が求められ、UDP通信が必要な場面。</p>
                        <span class="usecase-tag">NLB を選択</span>
                    </div>
                    <div class="usecase-card alb-use">
                        <h4>🏗️ マイクロサービス</h4>
                        <p>複数サービスを1つのALBで管理。/users → ユーザーサービス、/orders → 注文サービスなどパスで振り分け。</p>
                        <span class="usecase-tag">ALB を選択</span>
                    </div>
                    <div class="usecase-card nlb-use">
                        <h4>📡 IoT / リアルタイム通信</h4>
                        <p>大量のIoTデバイスからのTCP/UDP接続。毎秒数百万リクエストを処理する必要がある場面。</p>
                        <span class="usecase-tag">NLB を選択</span>
                    </div>
                    <div class="usecase-card alb-use">
                        <h4>🐳 コンテナ / Lambda</h4>
                        <p>ECS、EKS、Lambda などのサーバーレスやコンテナ環境。ALBの柔軟なターゲットタイプが活きる。</p>
                        <span class="usecase-tag">ALB を選択</span>
                    </div>
                    <div class="usecase-card nlb-use">
                        <h4>🔒 固定IPが必要な場面</h4>
                        <p>ファイアウォールでIP許可リストを設定する場合。DNS名ではなく固定IPでアクセスさせたい場面。</p>
                        <span class="usecase-tag">NLB を選択</span>
                    </div>
                </div>

                <!-- 判断フローチャート -->
                <div class="flowchart">
                    <h3 id="ALB-NLB-選択フローチャート">🔀 ALB / NLB 選択フローチャート</h3>
                    <div class="flow-steps">
                        <div class="flow-question">HTTPリクエストの中身（URL、ヘッダー）で<br>振り分けたい？</div>
                        <div style="display:flex;gap:40px;margin-top:0;">
                            <div class="flow-branch">
                                <span class="flow-label yes">✅ はい</span>
                                <span class="flow-down-arrow">⬇️</span>
                                <div class="flow-result alb-result">🔵 ALB を選択</div>
                            </div>
                            <div class="flow-branch">
                                <span class="flow-label no">❌ いいえ</span>
                                <span class="flow-down-arrow">⬇️</span>
                                <div class="flow-question" style="font-size:0.88em;padding:14px 20px;">超低遅延・静的IPが必要？<br>または TCP/UDP 通信？</div>
                                <div style="display:flex;gap:25px;margin-top:0;">
                                    <div class="flow-branch">
                                        <span class="flow-label yes">✅ はい</span>
                                        <span class="flow-down-arrow">⬇️</span>
                                        <div class="flow-result nlb-result">🟢 NLB を選択</div>
                                    </div>
                                    <div class="flow-branch">
                                        <span class="flow-label no">❌ いいえ</span>
                                        <span class="flow-down-arrow">⬇️</span>
                                        <div class="flow-result alb-result">🔵 ALB（汎用性）</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========== 構成パターン ========== -->
            <div class="section">
                <h2 id="️-典型的な構成パターン" class="section-title"><span class="emoji">🏛️</span> 典型的な構成パターン</h2>
                <p class="section-lead">実際の AWS 環境でよく使われる構成パターンを見てみましょう。</p>

                <!-- ALB 構成 -->
                <div class="architecture-diagram">
                    <h3 id="ALB-の典型構成-—-Webアプリ-+-マイクロサービス">🔵 ALB の典型構成 — Webアプリ + マイクロサービス</h3>
                    <div class="arch-row">
                        <div class="arch-box client">👥 ブラウザ<br>(HTTPS)</div>
                        <span class="arch-arrow">→</span>
                        <div class="arch-box alb-box">⚖️ ALB<br>(SSL終端)</div>
                        <span class="arch-arrow">→</span>
                        <div class="arch-target-group">
                            <div class="arch-box target">🖥 /api → APIサーバー</div>
                            <div class="arch-box target">🖥 /web → Webサーバー</div>
                            <div class="arch-box target">🖥 /admin → 管理画面</div>
                        </div>
                    </div>
                    <p class="arch-note">ALBがHTTPSを解読 → URLパスを確認 → 適切なターゲットグループへ転送</p>
                </div>

                <!-- NLB 構成 -->
                <div class="architecture-diagram">
                    <h3 id="NLB-の典型構成-—-ゲーム-リアルタイム通信">🟢 NLB の典型構成 — ゲーム / リアルタイム通信</h3>
                    <div class="arch-row">
                        <div class="arch-box client">🎮 ゲームクライアント<br>(TCP/UDP)</div>
                        <span class="arch-arrow">→</span>
                        <div class="arch-box nlb-box">⚡ NLB<br>(静的IP)</div>
                        <span class="arch-arrow">→</span>
                        <div class="arch-target-group">
                            <div class="arch-box target">🖥 ゲームサーバー #1</div>
                            <div class="arch-box target">🖥 ゲームサーバー #2</div>
                            <div class="arch-box target">🖥 ゲームサーバー #3</div>
                        </div>
                    </div>
                    <p class="arch-note">NLBが固定IPでリクエスト受付 → IP+ポートだけ見て → 超高速でサーバーへ転送</p>
                </div>
            </div>

            <!-- ========== 注意ポイント ========== -->
            <div class="section">
                <h2 id="⚠️-知っておきたい注意ポイント" class="section-title"><span class="emoji">⚠️</span> 知っておきたい注意ポイント</h2>

                <div class="tips-grid">
                    <div class="tip-card">
                        <h3 id="コスト感覚">💰 コスト感覚</h3>
                        <p>ALBの方がNLBより少し安いことが多いです。ただし、トラフィック量やルール数で変動するため、AWS料金計算ツールで事前に見積もりましょう。</p>
                    </div>
                    <div class="tip-card">
                        <h4>🔗 ALB + NLB の組み合わせ</h4>
                        <p>NLBの背後にALBを配置するパターンもあります。NLBの静的IPとALBの高度なルーティングを両立させる構成です。</p>
                    </div>
                    <div class="tip-card">
                        <h4>🏥 ヘルスチェック</h4>
                        <p>両方ともターゲットの健全性を監視します。異常なサーバーを自動的に除外し、復旧したら再登録。ALBはHTTPレベル、NLBはTCPレベルで確認します。</p>
                    </div>
                    <div class="tip-card">
                        <h4>🌍 クロスゾーン負荷分散</h4>
                        <p>複数のAZ（アベイラビリティゾーン）にまたがって負荷分散できます。ALBはデフォルトで有効、NLBはデフォルトで無効なので設定を確認しましょう。</p>
                    </div>
                </div>
            </div>

            <!-- ========== まとめ ========== -->
            <div class="summary-section">
                <h2 id="✅-まとめ">✅ まとめ</h2>
                <div class="summary-grid">
                    <div class="summary-card">
                        <div class="sum-icon">🔵</div>
                        <h3 id="ALB-を選ぶとき">ALB を選ぶとき</h3>
                        <p>HTTP/HTTPSのWebアプリを扱う。URLやヘッダーで賢くルーティングしたい。マイクロサービスやコンテナ環境で使う。</p>
                    </div>
                    <div class="summary-card">
                        <div class="sum-icon">🟢</div>
                        <h3 id="NLB-を選ぶとき">NLB を選ぶとき</h3>
                        <p>超低遅延が必要。TCP/UDPの生の通信を捌きたい。静的IPが必須。毎秒数百万件の超高トラフィックを処理する。</p>
                    </div>
                </div>
                <div class="summary-one-liner">
                    💡 迷ったら…  <strong>Webアプリなら ALB、それ以外で速さ・IP固定が要るなら NLB</strong>　が基本方針です！
                </div>
            </div>

        </div>
    

</div>

<!-- 固定ヘッダー機能のJavaScript -->
<script>
    // トップに戻るボタンの表示/非表示
    const scrollToTopBtn = document.getElementById('scrollToTop');
    const readingProgress = document.getElementById('readingProgress');
    const readingProgressBar = document.getElementById('readingProgressBar');

    if (scrollToTopBtn && readingProgress && readingProgressBar) {
        window.addEventListener('scroll', () => {
            const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrollPercentage = (scrollTop / scrollHeight) * 100;

            // トップに戻るボタンの表示制御
            if (scrollTop > 300) {
                scrollToTopBtn.classList.add('show');
            } else {
                scrollToTopBtn.classList.remove('show');
            }

            // 読書進捗バーの表示制御と更新
            if (scrollTop > 100) {
                readingProgress.classList.add('show');
                readingProgressBar.style.width = scrollPercentage + '%';
                readingProgress.setAttribute('aria-valuenow', Math.round(scrollPercentage));
            } else {
                readingProgress.classList.remove('show');
            }
        });

        // トップに戻る機能
        scrollToTopBtn.addEventListener('click', () => {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });
    }
</script>`;
---

<ResourceLayout {...frontmatter}>
  <Fragment set:html={rawContent} />
</ResourceLayout>
